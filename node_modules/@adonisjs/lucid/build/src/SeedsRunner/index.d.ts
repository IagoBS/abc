import { SeederFileNode } from '@ioc:Adonis/Lucid/Seeder';
import { QueryClientContract } from '@ioc:Adonis/Lucid/Database';
/**
 * Seeds runner exposes the API to list files from the seeds and
 * also run a collection of seeders
 */
export declare class SeedsRunner {
    private seedsDir;
    private isInDevelopment;
    constructor(seedsDir: string, isInDevelopment: boolean);
    /**
     * Returns an array of files inside a given directory. Relative
     * paths are resolved from the project root
     */
    listSeeders(): Promise<SeederFileNode[]>;
    /**
     * Returns an array of files inside a given directory. Relative
     * paths are resolved from the project root
     */
    run(seeder: SeederFileNode, client: QueryClientContract): Promise<SeederFileNode>;
}
